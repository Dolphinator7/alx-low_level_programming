#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 * @n: number being testedg
 *
 * Return: Value of the last digit.
 */
int print_last_digit(int n)
{
	int last_digit = n % 10; _putchar("\n last_digit %d = %d", n,
			last_digit)
	return 0;
}
