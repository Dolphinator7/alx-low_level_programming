#include <stdio.h>
#include "main.h"

/**
 * puts_half - prints half of the string
 *@str: string being tested
 *
 * return: void
 */

void puts_half(char *str)
{
	int len = 0;

	while (s[len] / 2 != '\0')
	{
		len++
	}
}
